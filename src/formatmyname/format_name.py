

def format_name(first_name, last_name):
    full_name = f'{first_name} {last_name}'
    # full_name = f'{first_name}'
    return full_name


def check_name(first_name, last_name):
    full_name = f'[OK]{first_name} {last_name}'
    # full_name = f'{first_name}'
    return full_name
